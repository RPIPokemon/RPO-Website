@require variables
@require nib
@require raleway
@require bootstrap

// angular-parallax
.background
    position relative
    background-attachment fixed
    background-position 50% 0
    background-repeat no-repeat

.home-background
    height calc(100vh - 50px)
    background-image url('img/rpi_photo.jpg')
